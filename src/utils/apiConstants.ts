// API request configuration constants
export const TYPING_DELAY_MS = 50; // Delay between character updates (~20 chars/sec)
export const STAGGER_DELAY_MS = 1000; // Delay between starting each request

